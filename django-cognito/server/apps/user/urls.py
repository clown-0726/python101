from django.conf.urls import url
from server.apps.user import views

urlpatterns = [
    # url(r'^logout$', views_login.UserLogoutView.as_view()),
    # url(r'^info$', views_info.UserInfoV3View.as_view()),
]
